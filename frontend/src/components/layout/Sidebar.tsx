'use client';

/**
 * Re-exporting the canonical Shadcn UI AppSidebar component.
 * Replaces the legacy custom sidebar implementation with the official Shadcn UI sidebar.
 */
export { AppSidebar, AppSidebar as Sidebar, default } from '@/components/app-sidebar';
